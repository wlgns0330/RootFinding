[project]
name = "yroots"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14t"
dependencies = [
    "matplotlib,
    "mpmath",
    "numba",
    "numpy",
    "scipy",
    "sympy",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
